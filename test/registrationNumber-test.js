describe('The Registration Number Plates function', function() 
{
   
    it('It should return the number plates array of the Town Given (Cape Town).',
    function() {

      var numbers = numberPlates({});
     });

     it('It should return the number plates array of the Town Given (Paarl).',
     function() {

       var numbers = numberPlates({});
      });

      it('It should return the number plates array of the Town Given (Worcester).',
      function() {

        var numbers = numberPlates({});
       });


      it('It should return the number plates that were stored that were from the Western Cape.',
       function() {
       var numbers = numberPlates({});
       });

       it('It should return the false for number plates that are not from the Western Cape.',
       function() {
       var numbers = numberPlates({});
       });

  });